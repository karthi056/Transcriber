name: speech_admin
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  process_run: ^0.13.0  # Moved from dev_dependencies

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1  # Adjusted version for compatibility

flutter:
  uses-material-design: true
  assets:
    - assets/ # Keeping the assets directory from the template